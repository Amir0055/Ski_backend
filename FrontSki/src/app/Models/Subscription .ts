export class Subscription {
    numSub!: number;
    startDate!: string;
    endDate!: string;
    price!: number;
    typeSub!: string; // Assuming TypeSubscription is a string, adjust accordingly
  }
  